import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parking.app',
  appName: 'parking-appHighBridge',
  webDir: 'dist/parking-app/browser'
  
};

export default config;
